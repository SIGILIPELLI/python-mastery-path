# Copy to .env (gitignored) — never commit real values.
# Populated automatically by oauth_setup.py; edit by hand only if needed.
LINKEDIN_ACCESS_TOKEN=
# Optional — auto-resolved from /v2/userinfo if left blank
LINKEDIN_PERSON_URN=
